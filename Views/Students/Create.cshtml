@model StudentViewModel
@{
    ViewData["Title"] = "Create student entity";
}
<h1>@ViewData["Title"]</h1>
<div class="row">
    <div class="col-md-4">
      <form asp-action="Create">
        <fieldset>
          <legend>Create student</legend>
          <div>
             <label for="id">Id:</label>
             <input type="number" name="id" value="" />
             @Html.ValidationMessage("id")
          </div>
          <div>
             <label for="name">Name:</label>
             <input type="text" name="name" value="" />
          </div>
          <div>
             <label for="gpa">Gpa:</label>
             <input type="number" step="0.01" name="gpa" value="" />
             @Html.ValidationMessage("gpa")
          </div>
          <div>
             <label>&nbsp;</label>
             <input type="submit" value="Submit" class="submit" />
          </div>
        </fieldset>
      </form>
    </div>
</div>
@if(@ViewBag.results!=null && @ViewBag.results!=""){
    <div class="text-center">
    <div class="alert alert-success" role="alert">
      @ViewBag.results
    </div>
    </div>
}

<div>
<a  asp-action="Index"> Back to student list</a>
</div>
